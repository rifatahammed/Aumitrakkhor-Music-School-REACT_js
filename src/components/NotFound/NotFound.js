import React, { useState } from "react";
import { Alert, Button } from "react-bootstrap";
import { render } from "react-dom";
import "./NotFound.css";
const NotFound = () => {
  return <h1>Not Found the page</h1>;
};

export default NotFound;
